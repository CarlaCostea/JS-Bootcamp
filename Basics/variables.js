let petName = 'Other';
// let petName = 'Other'; we can't define a variable more than once

petName = 'Tasha';

console.log(petName);

// There are rules related to variable names : they must start with letters or _ or $; no symbols accepted

//let test! = or let 2test = not accepted

let test_ = 1;
let $test = 2;

// no keywords as variables